using KomiteMobile.Views;

namespace KomiteMobile.Views.Operations;

public sealed class HistoryPage : MenuPlaceholderPage
{
    public HistoryPage()
        : base("Historial", "Registro de acciones operativas, supervisiones, cambios y evidencia.", "Modo Operacion")
    {
    }
}
